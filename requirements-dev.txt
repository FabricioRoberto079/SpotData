# Development and CI-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# The production image (Dockerfile) installs only requirements.txt.
pytest==8.4.2
httpx==0.28.1
pytest-cov==7.1.0
ruff==0.15.20
mypy==2.1.0
